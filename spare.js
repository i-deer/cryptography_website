/*function setup() {
    const ptext = document.getElementById("ptext").textContent
    const ctext = document.getElementById("ctext").textContent
    const key = document.getElementById("key").textContent
    const mode = document.getElementById("crypt_direction").textContent
    const cipher = document.getElementById("cipher_choice").textContent
    var value = choose_cipher(cipher, mode, ptext, ctext, key)

    if mode = "encrypt":
        ctext.innerHTML = value
    else:
        ptext.innerHTML = value
}*/

/* their tag : e.g. document.getElementsByTagName("p");
their ID : e.g. document.getElementById("header");
their class : e.g. document.getElementsByClassName("thumbnail") */